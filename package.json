{
  "name": "hackernews",
  "type": "module",
  "scripts": {
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --live-reload"
  },
  "dependencies": {
    "ziex": "^0.1.0-dev.987"
  },
  "devDependencies": {
    "wrangler": "^4.80.0",
    "@zigc/cli": "0.15.20"
  }
}